{
  "name": "obiimy-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Premium landing page versions for the Obiimy silk scarves brand",
  "scripts": {
    "start": "serve site -l ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
